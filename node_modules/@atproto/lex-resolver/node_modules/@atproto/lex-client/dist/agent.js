import { mergeHeaders } from './util.js';
export function isAgent(value) {
    return (typeof value === 'object' &&
        value !== null &&
        'fetchHandler' in value &&
        typeof value.fetchHandler === 'function' &&
        (!('did' in value) ||
            value.did === undefined ||
            typeof value.did === 'string'));
}
export function buildAgent(options) {
    if (typeof options === 'function') {
        return { did: undefined, fetchHandler: options };
    }
    const config = typeof options === 'string' || options instanceof URL
        ? { did: undefined, service: options }
        : options;
    // Prevent mismatch between `fetchHandler` and `headers` in the config, since
    // `headers` would be ignored if `fetchHandler` is provided.
    if ('fetchHandler' in config) {
        if (isAgent(config))
            return config;
        throw new TypeError('fetchHandler must not be provided when using AgentConfig');
    }
    const { did, service, fetch = globalThis.fetch, headers: defaultHeaders, } = config;
    if (typeof fetch !== 'function') {
        throw new TypeError('fetch() is not available in this environment');
    }
    return {
        get did() {
            return did;
        },
        async fetchHandler(path, init) {
            const headers = defaultHeaders != null && init.headers != null
                ? mergeHeaders(defaultHeaders, init.headers)
                : defaultHeaders || init.headers;
            return fetch(new URL(path, service), headers !== init.headers ? { ...init, headers } : init);
        },
    };
}
//# sourceMappingURL=agent.js.map